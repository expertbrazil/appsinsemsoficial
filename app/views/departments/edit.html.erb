<div class="card">
	<div class="card-header ch-alt m-b-20">  
		<%= link_to departments_path, "data-toggle": "tooltip", "data-placement":"bottom", "title": "Voltar", class: 'btn bgm-deeporange btn-float waves-effect' do %>
		<i class="zmdi zmdi-arrow-left"></i>
		<% end  if false%>

		<%= link_to @department, class: 'btn btn-primary waves-effect' do %>
		    <span class="glyphicon glyphicon-info-sign"></span>
		    Show
		<% end if false%>

		<h2><i class="fa fa-cubes"></i> <%= "Editando secretaria" %></h2>
	</div>

	<%= render 'form' %>
</div>
