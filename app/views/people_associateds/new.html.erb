<div class="card">
	<div class="card-header ch-alt">  
		<%= link_to people_associateds_path, "data-toggle": "tooltip", "data-placement":"bottom", "title": "Voltar", class: 'btn bgm-deeporange btn-float waves-effect' do %>
		<i class="zmdi zmdi-arrow-left"></i>
		<% end  if false%>
		<h2><i class="zmdi zmdi-account-box-o"></i> <%= "Novo #{CustomerConfiguration.current.menu_people_associateds.downcase}" %> <i class="c-gray"> <%= " - Matrícula #{@people_associated.number_registration.to_s}" %></i></h2>
	</div>

	<%= render 'form' %>
</div>
