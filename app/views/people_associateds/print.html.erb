<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Imprimir Inventários</title>
  
    <style type="text/css">

      @media print {
        a {
          text-decoration: none;
        }
      }

      body {
        font-size: 8pt;
      }
      table {
        width: 100%;
      }

      th {
        text-align: left;
        padding: 5px;
        border-bottom: 1px dashed #ccc;
      }

      td {
        padding: 5px;
        border-bottom: 1px dashed #ccc;
      }
      
      .line {
        border-left: 1px dashed #ccc;
        border-right: 1px dashed #ccc;
      }
    </style>
  </head>
  
  <body>
    <div id="header" class="row">
      <div>
        <h1>Relatório de <%= CustomerConfiguration.current.menu_people_associateds.titleize %></h1>
      </div>

      <button class="btn btn-success no-print" onclick="window.print();">Imprimir</button>
    </div>


    <br>
    <table class="table table-striped table-bordered table-hover">
      <thead>
        <tr>
          <th>Matricula</th>
          <th class="line"><%= filterrific_sorting_link(@filterrific, :name, { label: "Nome"}) %></th>
          <th>Observação</th>
        </tr>
      </thead>

      <tbody>
        <% @people_associateds.each do |people_associated| %>
          <tr>
            <td style="width: 50px;"><%= people_associated.number_registration %></td>
            <td class='line'><%= people_associated.name %></td>           
            <td><%= people_associated.observation %></td>    
          </tr>
        <% end %>
      </tbody>
    </table>

    <script type="text/javascript">
         window.onload = function() { 
          window.print();           
         }
    </script>
  </body>
</html>


