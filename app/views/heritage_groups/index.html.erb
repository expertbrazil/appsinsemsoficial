<div class="page-header">
  <%= link_to new_heritage_group_path, class: 'btn btn-primary waves-effect' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Heritage group
  <% end %>
  <h1>Listing heritage_groups</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
            <th>Name</th>
            <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <%= content_tag_for(:tr, @heritage_groups) do |heritage_group| %>
            <td><%= heritage_group.name %></td>
            <td><%= link_to 'Show', heritage_group %></td>
        <td><%= link_to 'Edit', edit_heritage_group_path(heritage_group) %></td>
        <td><%= link_to 'Destroy', heritage_group, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      <% end %>
    </tbody>
  </table>
</div>
