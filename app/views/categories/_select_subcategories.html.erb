<% options = Subcategory.select_options_grouped.map { |g| [g[0], g[1], {'data-type' => g[2]}] } %>
<%= select_tag "category[subcategory_id]", 
grouped_options_for_select(options), { class: "selectpicker", "data-live-search": true } %>
